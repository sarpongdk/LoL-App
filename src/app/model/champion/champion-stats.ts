export class ChampionStats {
   constructor(private armor: number, private attackDamage: number, private attackSpeed: number, private hp: number,
               private movementSpeed: number, private mp: number)
   {}
}
